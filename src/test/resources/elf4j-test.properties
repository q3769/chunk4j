#noop=true
level=info
writer1=standard
writer1.stream=stderr
#writer1.pattern={timestamp} {level:5} [{thread}] {class} - {message}
#writer1.pattern={json:caller-thread, caller-detail}
writer1.pattern={json:caller-thread, caller-detail}
#writer1.pattern={timestamp:yyyy-MM-dd'T'HH:mm:ss.SSSZZZZZ} {level:5} [{thread:name|id}] {class:simple|full|compressed}: {message}
